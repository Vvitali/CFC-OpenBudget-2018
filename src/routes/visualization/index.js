import React from "react";

import Map from '../../components/generalFundVisual';

const Visual = (prop)=>{
	return(
		<div>
		<h1>Visualization placeholder!</h1>
		<Map/>
		</div>
		);
};
export default Visual;